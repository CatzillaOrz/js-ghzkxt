/**
 * @description As a matter of fact, input out put is a side of effect that is communicating with the outside world in anyway is not pure
 * # with just pure functions, which are just functions that do something on the inside and outside world knows nothing about is philoosophically do anything
 * # because the program cannot exist without side effects.
 * # Pure function:
 * # 1 Task
 * # Predictable
 * # return statement
 * # Composable
 * # Pure
 * # Immutable State
 * # No shared state
 */
